* Math 클래스
 1. 수학 계산에 사용 할 수 있는 정적 메소드
  Type : int, double
  1) Type abs(Type a) : 절대값
  2) double ceil(double a) : 올림값
  3) double floor(double b) : 내림값
  4) Type max(Type a, Type b) : 최대값
  5) Type min(Type a, Type b) : 최소값
  6) double random() : 랜덤값
  7) double rint(double a) : 가까운 정수의 실수값
  8) long round(double a) : 반올림값

*Random 클래스
 1. boolean, int, long, float, double 난수를 얻을 수 있다.
 2. 난수를 만드는 알고리즘에 사용되는 종자값(Seed)를 설정할 수 있다.
  1) 정자값이 같으면 같은 난수를 얻는다.
 3. 객체 생성
  Random() 호출시 마다 다른 종자값(현재시간 이용)이 자동 설정된다.
  Random(long seed) 매개값으로 주어진 종자값이 설정된다.
 4. 제공 메소드
  nextBoolean() boolean 타입으로 난수를 리턴
  nextDouble() double 타입의 난수를 리턴( 0.0<= ~ <1.0 )
  nextInt() int 타입의 난수를 리턴(-2의 32승 <= ~ <= 2의 32승 -1);
  nextInt(int n) int 타입의 난수를 리턴 (0<= ~ <n)
